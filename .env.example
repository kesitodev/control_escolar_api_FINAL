
@'
# Variables de entorno para desarrollo local
# RENOMBRA ESTE ARCHIVO A .env Y CONFIGURA LOS VALORES

DEBUG=True
SECRET_KEY=django-insecure-clave-secreta-para-desarrollo-local
DATABASE_URL=postgresql://usuario:contraseña@ep-nombre.eastus2.aws.neon.tech/nombre_db?sslmode=require
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:4200,http://localhost:8000

# Configuración de correo (si usas)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
'@ | Out-File -FilePath .env.example -Encoding UTF8